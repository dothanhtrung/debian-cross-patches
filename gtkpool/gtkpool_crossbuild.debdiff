diff -u gtkpool-0.5.0/debian/rules gtkpool-0.5.0/debian/rules
--- gtkpool-0.5.0/debian/rules
+++ gtkpool-0.5.0/debian/rules
@@ -23,7 +23,7 @@
 config.status: configure
 # auto* calls are temporary, although, upstream seems a bit dead
 	autoreconf --install
-	CXXFLAGS="$(CFLAGS)" ./configure --prefix=/usr --sysconfdir=/etc \
+	dh_auto_configure -- CXXFLAGS="$(CFLAGS)" --prefix=/usr --sysconfdir=/etc \
                                          --mandir='$${prefix}/share/man'
 
 gtkpool/gtkpool:
